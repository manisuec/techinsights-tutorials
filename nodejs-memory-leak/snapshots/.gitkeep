# Heap snapshots and logs go here.
# Generated by scripts/snapshot.js, scripts/reproduce-leak.sh, etc.
# This directory is intentionally tracked (with .gitkeep) so clones have
# a place to write into without `mkdir`ing on first run.
